import { RemoveSymbolsDirective } from './remove-symbols.directive';

describe('RemoveSymbolsDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveSymbolsDirective();
    expect(directive).toBeTruthy();
  });
});
